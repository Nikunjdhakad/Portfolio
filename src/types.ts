
export interface Project {
  title: string;
  description: string;
  techStack: string[];
}

export interface Experience {
  title: string;
  role: string;
  description: string;
}

export interface Skill {
  name: string;
  category: string;
}
